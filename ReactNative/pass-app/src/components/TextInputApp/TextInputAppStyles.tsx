import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputer: {
        backgroundColor: '#e5b0f3',
        width: '100%',
        color: 'black',
        fontSize: 20,
        borderWidth: 1,
        borderColor: '#d2e716',
        borderRadius: 5,
        textAlign: 'center',
    }
});